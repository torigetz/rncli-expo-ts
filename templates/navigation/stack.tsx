
import React from 'react';
import { createStackNavigator } from '@react-navigation/stack';

const { Navigator, Screen } = createStackNavigator();

export const __name__: React.FC = () => {
    return (
        <Navigator>
            
        </Navigator>
    );
}
