
import React from 'react';
import { View } from 'react-native';
import generateStyles from './styles';

export interface I__name__ {}

export const __name__: React.FC<I__name__> = props => {
    const styles = generateStyles(props);

    return (
        <View>

        </View>
    )
}
